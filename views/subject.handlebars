{{!-- // This handlebars will make the particular Subject come into focus on the browser --}}
<div class="container">
  <h2 class="">{{subjectName}}</h2>
  <p class="">{{subjectInfo}}</p>
  <p class="">Created by {{user.name}} on {{format_date date_posted}}</p>
  {{#if logged_in}}
  <form class="form new-post-form">
      <div class="form-group">
        <label for="post-content">Comments?</label>
        <textarea class="form-input px-2" id="post-content" name="post-content"></textarea>
      </div>
      <div class="form-group">
        <button type="submit" class="btn btn-info" data-id="{{user_id}}"> Post Comment</button>
      </div>
    </form>
    {{else}}
    <p>Please <a href="/login">login</a> to leave a Comment! </p>
  {{/if }}
</div>
{{#if post.length}}
  <div>
    <div class="ctxt comment-list">
      <h3 class="ctxt">Current Comments:</h3>
      {{#each posts as |post|}}
    <div class="row mb-2 ctxt">
      <div class="col-md-6">
        <p id='posted-content'>{{post.postContent}}</p>
        <p>Comment date:{{post.postDate}}</p>
      </div>
    </div>
    {{/each}}
  </div>
</div>
{{/if}}

<script src="/js/subject.js"></script>
<script src="/js/logout.js"></script>